[build]
  command = "npm install"
  publish = "public"

# NOTA: Netlify solo sirve el frontend estático.
# El backend (API) corre en Render con server.js.
# Las functions inseguras sin auth fueron eliminadas (ver DEPLOY.md).
